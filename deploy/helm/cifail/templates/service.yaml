apiVersion: v1
kind: Service
metadata:
  name: {{ include "cifail.fullname" . }}
  labels:
    {{- include "cifail.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cifail.selectorLabels" . | nindent 4 }}
